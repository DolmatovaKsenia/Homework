//array 1
const
  size = 10;

var
  arr: array[1..size] of integer;
  factor, elem: integer;
  
begin
  for var i := 1 to size do 
  begin
    factor
    arr[i] := elem + factor * 2;
  writeln(arr)
end.